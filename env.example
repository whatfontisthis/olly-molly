# No environment variables required
# Image generation settings are configured in the app's Settings UI
# The app uses opencode or claude CLI tools for AI functionality

CODEX_CLI_ARGS="exec --dangerously-bypass-approvals-and-sandbox -"
